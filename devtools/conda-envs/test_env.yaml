name: test
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  # Testing
  - pytest
  - pytest-cov
  # Pip-only installs
  - pip:
    - codecov
    - catboost
    - lightgbm
    - pingouin
    - scikit-learn
    - scikit-posthocs
    - xgboost
    - torch
    - torchvision
    - torchaudio
