name: test
channels:
  - conda-forge
  - defaults
dependencies:
  - pip
  - rdkit
  # Testing
  - pytest
  - pytest-cov
  # Pip-only installs
  - pip:
    - codecov
    - catboost
    - deepchem
    - lightgbm
    - pingouin
    - scikit-learn
    - scikit-posthocs
    - xgboost
    - torch
    - torchvision
    - torchaudio
