name: test
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - rdkit
  # Testing
  - pytest
  - pytest-cov
  # Pip-only installs
  - pip:
    - codecov
    - catboost
    - deepchem
    - lightgbm
    - pingouin
    - scikit-learn
    - scikit-posthocs
    - xgboost
    - torch
    - torchvision
    - torchaudio
